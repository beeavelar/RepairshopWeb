@model IEnumerable<RepairshopWeb.Data.Entities.RepairOrder>

@{
    ViewData["Title"] = "Index";
}
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<h1><b>REPAIR ORDERS</b></h1>
<br />
<p>
    <a asp-action="Create" class="btn btn-primary" data-toggle="tooltip" title="Create New"><i class="fa-solid fa-plus"></i></a>
</p>
<br />
<table class="table" style="color: black; font-family: Arial; font-size: small">
    <thead style="text-transform: uppercase">
        <tr>
            @if (this.User.IsInRole("Admin"))
            {
                <th>
                    @Html.DisplayNameFor(model => model.User.FullName)
                </th>
            }
            <th style="width: 5%;">
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th style="width: 13%;">
                @Html.DisplayNameFor(model => model.RepairOrderDate)
            </th>
            <th style="width: 13%;">
                @Html.DisplayNameFor(model => model.Vehicle.LicensePlate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalServicesToDo)
            </th>
            <th style="width: 13%;">
                @Html.DisplayNameFor(model => model.Appointment)
            </th>
            <th style="width: 13%;">
                @Html.DisplayNameFor(model => model.AlertDate)
            </th>
            <th style="width: 13%;">
                @Html.DisplayNameFor(model => model.TotalToPay)
            </th>
            <th style="width: 13%;">
                @Html.DisplayNameFor(model => model.PaymentState)
            </th>
            <th style="width: 20%;"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @if (this.User.IsInRole("Admin"))
                {
                    <th>
                        @Html.DisplayFor(modelItem => item.User.FullName)
                    </th>
                }
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RepairOrderDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Vehicle.LicensePlate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalServicesToDo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Appointment)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AlertDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TotalToPay)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PaymentState)
                </td>
                <td>
                    <a asp-action="Appointment" asp-route-id="@item.Id" class="btn btn-info" id="btn_CreateAppointment" data-toggle="tooltip" title="Appoitment"><i class="fa-solid fa-calendar-days"></i></a> |
                    <a asp-action="DeleteRepairOrder" asp-route-id="@item.Id" class="btn btn-danger" id="btnDelete" data-toggle="tooltip" title="Delete"><i class="fa-solid fa-trash-can"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
